/**
 * extensions/deck.ts — Cribbage deck operations re-export.
 *
 * Re-exports deck functions from the shared cribbage deck module.
 */

export { freshDeck, dealCards, createDeck, shuffleDeck } from '../server/deck.js';
